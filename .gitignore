.DS_Store
.build/
DerivedData/
xcuserdata/
*.xcuserstate
.foreman/state.json
.foreman/postgres-history.json
*.profraw
*.profdata
.foreman/config.json
# sourcekit-lsp's build server, written by `xcode-build-server config`: absolute paths
# to this machine's DerivedData, and it is regenerated in one command.
buildServer.json
# and its flag cache, rewritten on every `xcode-build-server parse`
.compile
.compile.lock
