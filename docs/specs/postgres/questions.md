# Open questions

- [x] `cmd+shift+d` (the `postgres.schema` panel) clashed with `layout.split.horizontal`: the default is changed to `cmd+shift+b` (decision 2026-08-27); `architecture.md`, `00-study.md` and [`docs/shortcuts.md`](../../shortcuts.md) updated.
- [ ] Manual verification against a real server (M5 backlog, task 5.9): the server versions tested and the result of the checklist, to be recorded here.
