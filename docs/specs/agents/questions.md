# Open questions

- [x] Antigravity CLI binary: `agy` (2026-08-27).
- [ ] Automatic relaunch at restoration (e.g. `claude --continue`): a `"resume": true` option per agent? To be settled in use.
- [ ] A default shortcut for the main agent (e.g. `cmd+shift+a`), or none?
