# Open questions

- [ ] `sql` grammar (2026-08-27, R11, M5 task 5.4 plan B): `tree-sitter-sql` still does not resolve under Xcode 27 — `v0.3.11`'s test target names `"SwiftTreeSitter"` while the package identity is `swift-tree-sitter` (SwiftPM: *unknown dependency 'SwiftTreeSitter' in target 'TreeSitterSqlTests'*), and `main` (c2e1e08) is back on the deprecated `.package(name:)` that blocked M1. `.sql` files and the SQL editor stay plain text (R13). To revisit when upstream ships a manifest with `.product(name:package:)` or drops the test target's dependency; we do not patch a dependency. → **M6, Left out of v1 (watch upstream)**; the query editor uses `SQLHighlighter` meanwhile (postgres decision 2026-08-27).
- [ ] *(M7)* Formatting (2026-08-27, [01-study-formatter.md](01-study-formatter.md)): should `formatter.timeout` (R30, the 5 s bound) be a config key from v1, or do we wait until a slow `npx` gets cut off? Proposal: the key exists from v1, it costs three lines.
- [ ] *(M7)* Formatting (2026-08-27): should there be visible feedback when the formatter changed nothing (R28) — a brief status message, or nothing at all? To be settled in use.
- [ ] *(M7)* Formatting (2026-08-27, R28): should an entry be able to declare an accepted exit status, for the formatters that exit non-zero on a warning? Only `tidy` is known to need it (`format-all` accepts `(0 1)` for it and for nothing else), and `prettier` covers HTML anyway. Proposal: no — keep "exit `0` or nothing is applied", and revisit only if a second formatter needs it.
