import Testing

@testable import Wraith

/// Placeholder so the test target builds; replaced by real tests from task 0.3 on.
struct WraithTests {
    @Test func targetBuilds() {
        #expect(Bool(true))
    }
}
